Tools: 
      - No extra tools used besides CodeBlocks.
      - C Compiler (e.g., GCC, MinGW)
      - Make utility (for using the provided Makefile)

Libraries:
      - No external C libraries required (uses standard C library only).
      

  
